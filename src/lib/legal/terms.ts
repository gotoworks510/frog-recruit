/**
 * Version stamped when a user accepts Terms of Use + Privacy Policy.
 * Bump only when you intentionally require re-acceptance (not for routine edits).
 * First-login gate only checks that termsAcceptedAt is set — once is enough.
 */
export const TERMS_VERSION = "2026-09-16";
