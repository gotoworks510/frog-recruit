# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/

# opennext / cloudflare
/.open-next/
.wrangler/
.dev.vars

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files (secrets)
.env
.env*.local

# candidate PII — real seeded candidate data (emails, resume details). NEVER commit
# to a public repo. The data lives in D1; keep these files local only.
# (scripts/seed.sql = Palm company/job seed, no PII, IS committed.)
scripts/seed-*.sql

# typescript
*.tsbuildinfo
next-env.d.ts.bak

# drizzle
/drizzle/meta
